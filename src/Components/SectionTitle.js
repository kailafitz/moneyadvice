import React from 'react';

export default function SectionTitle({title}) {
    return (
        <div className="f-SectionTitle f-TextCenter">
            <h1 className="f-h2">{title}</h1>
        </div>
    )
}